---
description: Chrome DevTools MCP usage via Arc browser with remote debugging on port 9222.
alwaysApply: true
---

# Browser Testing via Chrome DevTools MCP + Arc

Use the `Chrome DevTools` MCP server.

## Workflow

1. `list_pages` to check if the URL is already open
2. If yes: `select_page` + `navigate_page` (use `type: "reload"` for same URL)
3. If no: `new_page` to open it
4. `take_screenshot` to verify the page loaded
5. Report what you see

## Connection issues -- Auto-update browser ID

The `Chrome DevTool` MCP might failed to connect. On connection error, fix automatically:

1. If `mcp.json` defined `--wsEndpoint`
- Run: `curl -s http://127.0.0.1:9222/json/version | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['webSocketDebuggerUrl'])"`
- If it succeeds: update `--wsEndpoint` in `~/.cursor/mcp.json` with the new `ws://...` URL
- Tell the user: "I've updated the browser ID in mcp.json. Please toggle the Chrome DevTools MCP server off and on in Cursor settings to reconnect."
- If port 9222 is unreachable: tell the user to relaunch Arc with `--remote-debugging-port=9222`

2. Otherwise, simply tell the user to check Chrome Devtools MCP.

## Error Handling

Follow the `autonomous-execution` rule. When Chrome DevTools MCP fails: state the problem, poll 30x at 10s intervals, resume or summarize. Never guess or fabricate browser results.

## Preventing OOM Crashes

The MCP server has a 8GB Node.js heap limit. Follow strictly:

- Never serialize the full DOM (`document.body.innerHTML`, `outerHTML`, etc.)
- Scope `evaluate_script` narrowly -- target specific selectors, return only needed data
- Prefer `take_screenshot` over `take_snapshot` (snapshots serialize the full accessibility tree)
- Use `take_snapshot` only on simple pages -- never on dashboards or large SPAs
- If the MCP crashes with OOM: tell user, reduce payload, do NOT retry the same large request

## Important

- Authentication usually need human intervention. Do NOT stop but ask user provide it by writing to signal file.
- Do NOT use `curl`, `open`, or `browser-use` subagent when Chrome DevTools MCP is available
